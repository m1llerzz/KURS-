# Единственная зависимость — драйвер Postgres.
#
# Почему она вообще появилась. На бесплатном тарифе Render диск стирается
# при каждом перезапуске и каждой заливке. Список подписчиков, который мы
# теряем раз в неделю, — это не неудобство, а конец продукта: единственный
# канал возврата людей исчезает молча.
#
# База — Neon, бесплатный тариф. Адрес кладётся в переменную DATABASE_URL.
# Без неё бот работает, но пишет подписчиков в файл и предупреждает об этом
# в журнале при каждом запуске.
psycopg2-binary==2.9.9
